
isFruit ("radio")

function isFruit (name){

    if (name == "apple") {
        console.log (name + ' is a fruit')
    }
    else if (name == "banana") {
        console.log(name)
    }
    else {
        console.log("no fruites " + name)
    }
}